{
  "name": "threejs-typescript-starter",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun run --bun tsc && bun run --bun vite build",
    "preview": "bun run --bun vite preview"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@types/three": "^0.181.0",
    "typescript": "^5.3.3",
    "vite": "^7.1.12"
  }
}
